{
  "name": "@holdem/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Headless, scriptable engine harness: plays one hand vs the bots from a seed + scripted hero actions and prints a deterministic transcript — a smoke-test driver, not the play UI.",
  "scripts": {
    "sim": "tsx src/sim.ts",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@holdem/bots": "workspace:*",
    "@holdem/coach": "workspace:*",
    "@holdem/engine": "workspace:*",
    "@holdem/format": "workspace:*",
    "@holdem/odds": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.22.4"
  }
}
