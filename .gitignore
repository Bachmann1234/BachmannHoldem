node_modules/
dist/
.svelte-kit/
build/
coverage/
*.tsbuildinfo
.DS_Store
.env
.env.*

# The user's global gitignore excludes all of .claude/. Re-include project skills so they're
# version-controlled with the repo, while keeping machine-specific .claude/ files ignored.
!.claude/
.claude/*
!.claude/skills/

# Playwright MCP scratch (screenshots/snapshots from manual verification)
.playwright-mcp/
