# Bugs

Defect tickets. One file per bug: `BUG-NNNN-short-slug.md` (copy [`TEMPLATE.md`](TEMPLATE.md)).

Lifecycle via the `status` field: `open → in-progress → fixed` (or `wontfix`). List open bugs:

```bash
grep -rl 'status: open' tickets/bugs/
```

No open bugs yet.
