# Lint + format only the staged files (fast).
#
# Direct commits to main are allowed: the real gate is the pre-push hook, which runs the full
# `pnpm verify` (format + lint + typecheck + tests) before anything leaves the machine — the same
# suite CI runs. Branch-per-feature is still the norm, but it's no longer hard-blocked here.
pnpm exec lint-staged
